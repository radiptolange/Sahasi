// --- Global Configs and Keys ---
// This class holds constant keys used for storing settings in the app.
class AppConfig {
  // Key for auto-recording setting
  static const String keyAutoRecord = 'autoRecord';
  // Key for sharing location setting
  static const String keyShareLocation = 'shareLocation';
  // Key for double tap SOS setting
  static const String keyDoubleTapSos = 'doubleTapSos';
  // Key for language setting
  static const String keyLanguage = 'language';
  // Key for SOS SMS interval setting
  static const String keySosIntervalSeconds = 'sosIntervalSeconds';
  // Key for fake call delay setting
  static const String keyFakeCallDelay = 'fakeCallDelay';
}
